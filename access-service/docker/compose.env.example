# Copy to access-service/.env. Replace with the digest shown in GitHub Actions.
CLI_ACCESS_IMAGE=ghcr.io/mossexplore/cli-access:main
# Host Nginx: keep loopback. Direct intranet access: set the server's intranet IP.
BIND_IP=127.0.0.1
HTTP_PORT=8008
