# AGENTS.md

## CLI 设计强制约束

每次设计、实现或修改 CLI 命令时，必须遵守以下约束：

1. **业务请求头**：新增 CLI 命令调用接口时，请求头必须携带 `businessid`。其值必须读取自 `business.json` 中当前具体环境的 `selected.businessId`，不得硬编码或使用其他环境的值。
2. **时间展示**：创建时间、更新时间等字段若采用含 `T`、毫秒、时区偏移等不便阅读的格式，必须转换为适合人类阅读的 `YYYY-MM-DD HH:mm:ss` 格式。按北京时间（UTC+8）展示，并正确转换时区，而非仅替换分隔符。例如，`2026-09-08T06:54:36.000+00:00` 应展示为 `2026-09-08 14:54:36`。
3. **响应示例完整性**：用户提供的响应体可能仅展示部分数据，其他无关数据可能已省略。不得将示例视为完整响应结构，也不得因示例未展示某字段而假定实际接口不存在该字段。
4. **首列 ID 展示**：若第一列展示 ID，该列宽度必须固定为 `36`，不得换行、截断或省略 ID；其他长字段优先换行，以便用户完整复制第一列的 ID。
5. **接口地址来源**：用户给出的请求 URL 若包含 `xxx` 占位符，`xxx` 必须取自 `config.json` 中的 `api_endpoint` 值，不得硬编码为其他地址。
6. **使用指南同步**：若新增 CLI 命令，必须同步更新 `docs/CLI参考使用指南.md`，添加新命令的详细使用指导，包括命令用途、使用前提、参数与选项、调用示例及输出说明。
7. **新增命令版本递增**：若新增 CLI 命令，必须主动将打包版本和 CLI 版本的补丁号同步加 `1`，无需用户另行提醒。例如，`0.3.27` 升级为 `0.3.28`。同步修改 `pyproject.toml` 的 `project.version` 和 `src/wiserec_cli/__init__.py` 的 `__version__`，确保两者一致，并更新相关版本测试及文档中的当前版本说明。一次变更包含多个新增命令时统一递增一次；若本次变更已完成版本递增，不重复递增。打包后必须确认包元数据与 `ml --version` 均为新版本。

## Git 提交与 Push

- 代码完成后，必须先完成测试并确认验证通过，再执行 `git commit`。
- Push 到远程时，必须直接 Push 到 `main` 分支。
- 禁止 Push 到其他分支。

## 上下文压缩

整理或压缩当前任务上下文时：

- 清理无效信息。
- 删除或标记已废弃的方案。
- 删除已完成且无需继续参考的内容。
- 删除与当前目标无关的历史记录。
- 删除低价值的重复输出。
- 重新生成精简任务摘要，仅保留：
  - 当前目标
  - 核心约束
  - 已确认决策
  - 当前进度
  - 未解决问题
  - 下一步计划
- 后续执行任务时，以最新任务摘要为主要依据，不依赖已失效的历史信息。

## 交互与可见性

- 涉及网页搜索、网页操作或视觉检查时，打开并保持 Codex 右侧浏览器面板可见，以便用户旁观和参与决策。
- 纯本地代码、文件操作和文本搜索可以在后台执行，无需打开浏览器面板。
- 遇到关键方案选择或不可逆操作时：
  1. 先说明可选方案及主要取舍。
  2. 等待用户确认后再执行。
- 每完成一个关键阶段，简短汇报：
  - 当前发现
  - 下一步计划

## 模块大小与可维护性

- 单个模块原则上必须小于 `500 LoC`，测试代码不计入。
- 单个文件接近 `800 LoC` 时，必须考虑并执行拆分，创建新的 module。
- 拆分时应保持职责清晰，避免通过简单搬移代码制造无意义的模块。
